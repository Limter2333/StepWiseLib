[
  {
    "description": "TokenBucket algorithm correctly limits burst requests",
    "steps": [
      "Create TokenBucket with capacity=3",
      "Consume 3 tokens",
      "Verify next consume is rejected"
    ],
    "passes": true
  },
  {
    "description": "SlidingWindowCounter tracks requests per time window",
    "steps": [
      "Create counter with max_requests=5",
      "Make 5 requests",
      "Verify 6th request is blocked"
    ],
    "passes": true
  },
  {
    "description": "RateLimiter blocks clients exceeding configured limits",
    "steps": [
      "Configure RateLimiter with requests_per_minute=5",
      "Make 10 rapid requests",
      "Verify requests after limit are rejected with 429"
    ],
    "passes": true
  },
  {
    "description": "Whitelisted clients bypass rate limiting",
    "steps": [
      "Add client IP to whitelist",
      "Make requests from that client",
      "Verify all requests are allowed"
    ],
    "passes": true
  },
  {
    "description": "RateLimiter tracks client statistics",
    "steps": [
      "Make requests from a client",
      "Call get_client_stats()",
      "Verify total_requests and blocked_requests are tracked"
    ],
    "passes": true
  }
]
