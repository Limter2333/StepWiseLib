[
  {
    "description": "AlertManager can add and remove alert rules",
    "steps": [
      "Create an AlertRule with event_type, condition, level",
      "Add rule via add_rule()",
      "Verify rule appears in list_rules()",
      "Remove rule via remove_rule()",
      "Verify rule no longer appears"
    ],
    "passes": true
  },
  {
    "description": "Events that meet condition trigger alert creation",
    "steps": [
      "Add rule with condition=lambda d: d.get('count', 0) >= 3",
      "Process event with count=5",
      "Verify alert is created and active"
    ],
    "passes": true
  },
  {
    "description": "Alert cooldown prevents duplicate alerts within window",
    "steps": [
      "Add rule with cooldown_seconds=60",
      "Process event to trigger alert",
      "Immediately process another event",
      "Verify only one alert is created (cooldown blocks second)"
    ],
    "passes": true
  },
  {
    "description": "Threshold aggregation counts events before firing",
    "steps": [
      "Add rule with threshold=3, aggregation_key='agent'",
      "Process 3 events with same agent",
      "Verify alert is created with event_count=3"
    ],
    "passes": true
  },
  {
    "description": "Alert channels receive notifications when alerts fire",
    "steps": [
      "Add a mock channel to manager",
      "Process event that triggers alert",
      "Verify channel.send() was called with the alert"
    ],
    "passes": true
  }
]
