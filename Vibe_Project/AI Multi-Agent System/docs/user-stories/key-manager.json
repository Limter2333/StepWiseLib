[
  {
    "description": "SecretStore can store and retrieve encrypted secrets",
    "steps": [
      "Call store() with name, value, key_type, client_id",
      "Verify key_id is returned",
      "Call retrieve() with same name",
      "Verify the original value is returned"
    ],
    "passes": true
  },
  {
    "description": "Duplicate key names are rejected",
    "steps": [
      "Store a key with name='test'",
      "Attempt to store another key with same name",
      "Verify ValueError is raised"
    ],
    "passes": true
  },
  {
    "description": "Revoked keys cannot be retrieved",
    "steps": [
      "Store a key",
      "Call revoke() on the key",
      "Attempt to retrieve the key",
      "Verify PermissionError is raised"
    ],
    "passes": true
  },
  {
    "description": "Key metadata and use_count are tracked",
    "steps": [
      "Store a key with metadata={'env': 'prod'}",
      "Retrieve the key twice",
      "Call get_info()",
      "Verify metadata is preserved and use_count is 2"
    ],
    "passes": true
  },
  {
    "description": "Audit log records store and retrieve actions",
    "steps": [
      "Store a key",
      "Retrieve the key",
      "Call get_audit_log()",
      "Verify both 'store' and 'retrieve' actions are logged"
    ],
    "passes": true
  }
]
