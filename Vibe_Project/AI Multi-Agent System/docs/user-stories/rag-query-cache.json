[
  {
    "description": "RAG query endpoint returns cached result on second call with same question",
    "steps": [
      "POST /api/rag/query with question='什么是Python' (first call)",
      "Verify response.cached is False",
      "POST /api/rag/query with same question (second call)",
      "Verify response.cached is True"
    ],
    "passes": true
  },
  {
    "description": "Cache stats endpoint returns hit/miss statistics",
    "steps": [
      "GET /api/rag/cache/stats",
      "Verify response contains hits, misses, sets, hit_rate fields"
    ],
    "passes": true
  },
  {
    "description": "Cache can be cleared via API endpoint",
    "steps": [
      "DELETE /api/rag/cache/clear",
      "Verify response.success is True",
      "GET /api/rag/cache/stats shows memory_entries = 0"
    ],
    "passes": true
  },
  {
    "description": "Query with use_cache=False bypasses cache",
    "steps": [
      "POST /api/rag/query with question='test' and use_cache=False",
      "Verify response.cached is undefined or False"
    ],
    "passes": true
  },
  {
    "description": "Different questions generate different cache entries",
    "steps": [
      "Cache question A",
      "Query question B",
      "Verify question B is not cached from A"
    ],
    "passes": true
  }
]
